<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.trc.mapper.order.IMembershipDcoreDailyDetailsMapper">
    <select id="selectListByParams" parameterType="org.trc.domain.dto.SettlementQuery" resultType="org.trc.domain.order.MembershipScoreDailyDetailsDO">
        select * from membership_score_daily_details
        <where>
            <if test="userId != null ">
                AND userId = #{userId}
            </if>
            AND <![CDATA[ UNIX_TIMESTAMP(accountDay) >= unix_timestamp(#{startTime})
            AND UNIX_TIMESTAMP(accountDay) < unix_timestamp(#{endTime})]]>
        </where>
    </select>

    <select id="queryMembershipScoreDailyDetailForExport" parameterType="org.trc.domain.dto.SettlementQuery" resultType="org.trc.domain.order.MembershipScoreDailyDetailsDO">
        select * from membership_score_daily_details
        <where>
            <if test="userId != null ">
                AND userId = #{userId}
            </if>
            AND <![CDATA[ UNIX_TIMESTAMP(accountDay) >= unix_timestamp(#{startTime})
            AND UNIX_TIMESTAMP(accountDay) < unix_timestamp(#{endTime})]]>
        </where>
    </select>
</mapper>
